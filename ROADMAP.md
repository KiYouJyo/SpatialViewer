# Spatial Viewer 路线图

> 本文描述开发方向，不代表发布日期承诺。具体优先级会根据内核验证结果与兼容性问题调整。

## 0. 项目基础

- Stage 1 已完成：格式无关 Core、Scene2D、双精度坐标、Win2D Debug Host、自动测试、benchmark 与 Windows CI；正式多标签应用框架仍在后续阶段
- 简体中文、日本語、English 本地化基础
- 文件类型识别、错误处理与诊断信息
- 通用视图状态、图层/对象信息模型与渲染接口
- 测试、CI、打包与发布基础设施

## 1. CAD 查看

Stage 2 已建立 DXF/DWG 的基础导入链路：自有 CAD model、可替换 reader adapter、基础二维图元/图层/颜色/文字/block 与真实 fixture 验证。目标仍是逐步覆盖更多常见图元、线型和布局行为；对代理对象、第三方插件对象和外部依赖采用明确的兼容性边界。

## 2. GIS 查看

目标是支持常见矢量、栅格和地图数据的浏览，逐步完善图层、坐标参考、样式、属性、空间范围与地图显示能力；需要联网的底图或服务与本地数据能力保持解耦。

## 3. BIM / IFC 查看

以 IFC 为主要交换入口，逐步实现模型几何、空间/构件层级、分类、属性和基础选择查询能力。Revit 原生项目格式不等同于 IFC 支持范围。

## 4. Rhino / 3DM 查看

逐步实现 3DM 几何、图层、对象属性、材质及常见视图信息的读取与显示。

## 5. 统一查看体验

在各内核可独立验收后，统一完善：

- 标签页与最近文件
- 视图导航、选择、框选、缩放与定位
- 图层/对象树与属性面板
- 测量、坐标、查询等轻量查看工具
- 大文件性能、增量加载与渲染稳定性
- 格式兼容性矩阵和回归测试样例

## 兼容性原则

1. “可以打开”与“完整显示”分开验收。
2. 每种格式都维护明确的支持矩阵和已知限制。
3. 不用未经验证的宣传替代真实样例测试。
4. 第三方插件对象、字体、外部参照、在线服务等外部依赖单独记录。
5. 对无法合法分发的测试文件，仅保留脱敏后的最小复现样例或测试描述。
