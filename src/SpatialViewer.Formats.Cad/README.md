# Moved to SpatialViewer.CadCore

CAD semantic model and scene translation moved to `external/SpatialViewer.CadCore/src/SpatialViewer.Formats.Cad`. This directory is a temporary solution compatibility marker only.
