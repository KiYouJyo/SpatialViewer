# Moved to SpatialViewer.CadCore

Backend-neutral rendering code moved to `external/SpatialViewer.CadCore/src/SpatialViewer.Rendering`. This directory is a temporary solution compatibility marker only.
