# Moved to SpatialViewer.CadCore

Kernel implementation moved to `external/SpatialViewer.CadCore/src/SpatialViewer.Core`. This directory remains temporarily as a solution compatibility marker and contains no authoritative kernel source.
